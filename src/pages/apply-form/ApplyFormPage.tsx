import { ApplyFormContainer } from '@/features';

import { PageWrapper } from '@/widgets';

export const ApplyFormPage = () => {
  return (
    <PageWrapper>
      <ApplyFormContainer />
    </PageWrapper>
  );
};
